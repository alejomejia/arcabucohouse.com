/**
 * HTML id prefix that pairs a `<Tabs.Trigger>` with its `<Tabs.Content>`
 * panel. Kept centrally so the `aria-controls` / `aria-labelledby` chain
 * has a single source of truth.
 */
export const TABS_TRIGGER_ID_PREFIX = 'tab'

/** HTML id prefix for the `<Tabs.Content>` panel. */
export const TABS_PANEL_ID_PREFIX = 'tabpanel'
